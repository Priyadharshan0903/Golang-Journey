package main

import "fmt"

func main() {

	// multiple ways to initialize any array
	// using make and just by declaring them in the curly bracket with/without denoting the size of it
	var sample = [3]string{"hello", "mosi mosi", "konichiwa"}

	fmt.Println(sample)
}
